upstream api_server {
     server api;
}


map $http_x_service $services {
     default "api_server";
}

server {
    listen 80;
    
    location / {
        proxy_pass_request_headers      on;
        proxy_pass http://$services;
    }
}
